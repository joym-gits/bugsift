Summarise the role of this file in its codebase in 1–2 sentences.

Focus on:
- what the file exposes (main functions, classes, endpoints)
- what problem it solves / what layer it belongs to

Do NOT speculate beyond what's shown. Do NOT quote code. If the file
looks empty or trivial, say so.

Path: `{{ file_path }}`
Language: {{ language or "unknown" }}

Content:
```
{{ content }}
```

Respond with only the summary text — no headings, no bullet points.
