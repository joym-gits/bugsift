You are generating a minimal {{ language }} script that reproduces a reported bug inside a hardened ephemeral sandbox.

Sandbox constraints:
- {{ language }} interpreter is pre-installed (Python 3.11 / Node 20).
- Read-only root filesystem. Only /tmp is writable (tmpfs, 64MB).
- 60-second hard wall-clock timeout, 512MB memory, 50 processes.
- Network access is allowed so `pip install` / `npm install --global` can pull dependencies — but avoid needing network when the reported bug can be reproduced with stdlib only.
- No GUI. No interactive input. No long-running servers.

Script rules:
- Self-contained and runnable as `python /tmp/script.py` (or `node /tmp/script.js`).
- If the bug is present, the script should exit NON-ZERO or raise / throw. If the bug is absent, exit 0 cleanly.
- Print the relevant error or observed behaviour to stderr/stdout so a judge can tell what happened.
- Under 60 lines. Prefer stdlib. If you must install a dep, do it at the top in code.
- Never write outside /tmp. Never attempt privilege escalation. Never open interactive prompts.

You MUST respond with exactly one JSON object and nothing else — no markdown fences, no prose. Schema:

{"script": "<full source>", "expected_markers": ["<string likely to appear in output if the bug is reproduced>"], "rationale": "<one short sentence>"}

If you cannot reliably construct a minimal reproduction from the issue content, return:
{"script": "", "expected_markers": [], "rationale": "insufficient information to reproduce"}

---
Repository: {{ repo_full_name }}
Primary language: {{ repo_primary_language or "unknown" }}
Classifier rationale: {{ rationale or "(no rationale)" }}

Issue title: {{ issue_title }}

Issue body:
{{ issue_body or "(empty)" }}
